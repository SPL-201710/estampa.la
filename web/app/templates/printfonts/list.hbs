{{outlet}}
<div class="container">
  <div class="col-md-12">
    <div class="pagenav-wrap">
      <div class="row">
        <div class="col-md-12 col-sm-12">
          <div class="pull-right">
            {{#link-to "printfonts.add" class="btn btn-success"}}Agregar Tipo de Letra{{/link-to}}
          </div>
        </div>
      </div>
    </div>
    <div class="space50"></div>
    <div class="row">
      <table class="table">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Editar</th>
            <th>Eliminar</th>
          </tr>
        </thead>
        <tbody>
          {{#each model as |tipoletra|}}
          <tr>
            <td>{{tipoletra.name}}</td>
            <td>{{#link-to "printfonts.edit" tipoletra.id class="btn btn-info"}}Editar{{/link-to}}</td>
            <td><button {{action "deletePrintFont" tipoletra}} class="btn btn-danger">Eliminar</button></td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>
