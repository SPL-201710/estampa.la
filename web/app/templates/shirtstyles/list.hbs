{{outlet}}
{{outlet}}
<div class="container">
  <div class="col-md-12">
    <div class="pagenav-wrap">
      <div class="row">
        <div class="col-md-12 col-sm-12">
          <div class="pull-right">
            {{#link-to "shirtstyles.add" class="btn btn-success"}}Agregar Estilo{{/link-to}}
          </div>
        </div>
      </div>
    </div>
    <div class="space50"></div>
    <div class="row">
      <table class="table">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Editar</th>
            <th>Eliminar</th>
          </tr>
        </thead>
        <tbody>
          {{#each model as |estilo|}}
          <tr>
            <td>{{estilo.name}}</td>
            <td>{{#link-to "shirtstyles.edit" estilo.id class="btn btn-info"}}Editar{{/link-to}}</td>
            <td><button {{action "deleteShirtStyle" estilo}} class="btn btn-danger">Eliminar</button></td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>
