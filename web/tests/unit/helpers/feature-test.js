
import { feature } from 'estampala/helpers/feature';
import { module, test } from 'qunit';

module('Unit | Helper | feature');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = feature([42]);
  assert.ok(result);
});

